# AGENTS.md

이 문서는 이 저장소에서 작업하는 에이전트가 따라야 할 기본 규칙을 정리합니다.

## 작업 기준

- 새 요청을 처리할 때는 이전 대화, 초안, 마지막으로 수정했던 내용을 기준으로 추정하지 않고, 작업 대상 파일의 현재 내용을 먼저 확인합니다.
- 사용자가 초안이나 이전 작업 결과물을 직접 수정했을 수 있으므로, 작업 전 대상 파일의 현재 내용을 최신 원본으로 간주합니다.
- 코드, 문서, 설정을 설명하거나 수정할 때는 현재 파일 내용을 기준으로 판단합니다.
- 현재 파일 내용과 이전 작업 기억이 다르면 현재 파일 내용을 우선합니다.
- 사용자가 직접 수정한 내용을 예전 초안이나 이전 작업 상태로 되돌리지 않습니다.

## 코드 리뷰 기준

- 코드 리뷰 요청을 받으면 프로젝트 로컬 `code-review/SKILL.md`가 있는지 확인하고, 있으면 해당 파일의 현재 내용을 먼저 읽은 뒤 그 기준으로 리뷰합니다.
- `code-review/SKILL.md`도 작업 대상 파일과 마찬가지로 이전 기억이 아니라 현재 파일 내용을 최신 기준으로 간주합니다.

## PR 기록 기준

- 코드 리뷰, 코드 검토, 동작 오류 조사, 버그 수정처럼 원인 분석과 수정 근거가 중요한 작업은 가능한 한 별도 브랜치와 GitHub PR로 기록을 남깁니다.
- PR 본문에는 문제 상황, 검토한 내용, 실제 수정 내용, 검증 결과를 간단히 정리합니다.
- 사용자가 명시적으로 바로 커밋/푸시만 요청했거나 변경이 없는 단순 설명 작업이면 그 요청과 현재 작업 범위를 우선합니다.

## 커밋 원칙

- 커밋 메시지는 한국어로 작성합니다.
- 하나의 커밋에는 하나의 의도만 담습니다.
- 기능 추가, 버그 수정, 리팩터링, 문서 변경처럼 성격이 다른 작업은 커밋을 분리합니다.
- 같은 목적의 변경은 파일이 여러 개여도 하나의 커밋으로 묶습니다.
- 사용자가 직접 만든 변경사항은 되돌리거나 덮어쓰지 않습니다.
- 커밋 전에는 `git status`와 `git diff`로 포함될 변경 범위를 확인합니다.

## 커밋 메시지 작성 규칙

- 제목은 변경의 핵심 목적을 짧고 구체적으로 작성합니다.
- 제목만 보고도 무엇이 바뀌었는지 알 수 있어야 합니다.
- 본문에는 실제 변경 내용, 변경 이유, 영향 범위를 필요한 만큼만 적습니다.
- 변경하지 않은 파일이나 관련 없는 작업은 언급하지 않습니다.
- 단순 포맷팅, 오타 수정처럼 영향이 작은 변경은 본문을 생략할 수 있습니다.
- 의미 있는 검증을 수행했다면 본문에 결과를 남깁니다.
- 매번 반복되는 범위 확인성 검증처럼 중요도가 낮은 검증은 커밋 본문에 포함하지 않습니다.
- 검증하지 못했다면 그 이유를 커밋 본문이나 작업 요약에 명확히 남깁니다.

## 권장 형식

```text
커밋 제목

- 변경 내용:
  - 실제로 수정한 범위를 구체적으로 작성
- 변경 이유:
  - 이 변경이 필요한 이유나 사용자에게 미치는 영향 작성
- 검증:
  - 실행한 테스트, 수동 확인, 또는 검증하지 못한 이유 작성
```

## 예시

```text
동영상 속도 패널 표시 방식 개선

- 변경 내용:
  - 콘텐츠 스크립트에서 속도 패널 렌더링 위치를 조정
  - 팝업 스타일과 패널 간격을 정리
- 변경 이유:
  - 사이트별 레이아웃과 겹치는 문제를 줄이고 가독성을 개선
- 검증:
  - YouTube와 Netflix 화면에서 패널 표시 동작을 수동 확인
```
